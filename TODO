[ ] what if router is unreachable
    1. drop active client connections/refuse client connections
    2. reject stanzas immediately (stream error)
    3. queue stanzas for future sending
[ ] what if default route has not been assigned
    1. drop stanza
    2. send error back to component
